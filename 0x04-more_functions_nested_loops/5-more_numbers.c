#include "holberton.h"

/**
 * more_numbers - keep printing nums.
 *
 * Return: Always 0.
 */
void more_numbers(void)
{
	int i, j, l, k;

	for (j = 0; j < 10; j++)
	{
		for (i = 0; i <= 14; i++)
		{
			if (a > 9)
				_putchar(a / 10 + '0');
			_putchar(a % 10 + '0');
		}
		_putchar('\n');
	}
}
